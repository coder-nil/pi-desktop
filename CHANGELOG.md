# Changelog

所有重要变更都会记录在此文件中。

## [0.85.1-alpha.13] - 2026-09-22

### Added

- 手机遥控页支持公网访问：新增公网入口开关、域名自动发现与隧道状态展示，桌面设置与手机配对共用同一个二维码，并支持已保存密码的掩码显示与访问日志查看。
- 手机在同 Wi-Fi 下自动切回局域网地址：新增服务端来源判定与一次性跳转页，外网访问保留继续入口。
- Git 面板的提交信息改为流式生成并逐字显示：新增 NDJSON 流式响应（更新 / 完成 / 错误），生成过程中保留错误反馈。
- 「全部丢弃」改为真正恢复工作区：重置暂存区、还原已跟踪文件并清理未跟踪文件；确认框改为应用内对话框，按名单列出将被删除的未跟踪文件。
- 新增 `docs/mobile-public-access.md` 与中文版，说明 `/m` 从同一 Wi-Fi 扩展到任意网络的方案、安全项与 Cloudflare Tunnel 操作手册。

### Changed

- 侧边栏会话项排版调整：会话行改为 50px 可视高度加 2px 间距，选中/删除态由 `border-left` 改为 `inset box-shadow`，悬浮操作按钮改为右侧浮层并用渐变遮罩替代硬边裁切。
- 移除侧边栏右侧边框，为会话列表补充上下内边距。
- 应用、npm、Tauri 和 Cargo 版本号统一为 `0.85.1-alpha.13`。

### Fixed

- 修复 `package-lock.json` 丢失 `qrcode.react` 条目导致 `npm ci` 报 `EUSAGE` 失败：该依赖被 `MobileAccessSettings` 与 `MobilePairDialog` 使用，lock 需与 `package.json` 重新同步。
- 修复「全部丢弃」在只有已暂存或未跟踪文件时仍然显示、点了却没有效果的问题。
- 修复 `discard` 对已暂存的重命名/复制无效：索引残留导致工作区还原不生效。
- 修复「手机查看」入口在新建会话（还没有 sessionId）时就出现的问题：现在等会话产生后再显示。
- 修复 `set_model` 因旧 `models.json` 遗留的 `baseUrl`/`api` 配置导致请求 404：先做一次仅缓存的模型刷新再解析模型。
- 修复卸载或切换会话时主动关闭事件流被误报为错误并触发重试提示。

## [0.85.1-alpha.12] - 2026-09-19

### Added

- 手机遥控页（/m）重做展示层：助手正文按 markdown 渲染（代码块带语言标签与复制、表格横滚），用户消息改为右对齐气泡，补齐骨架屏、空态、错误条与「回到最新」。
- 手机遥控页新增「处理详情」折叠组：思考过程与工具调用（入参与结果可单独展开）折进折叠组，运行中展开、结束自动收起。
- 手机遥控页支持往回翻历史：窗口默认最新 30 条、最多 300 条，配有「载入更早的消息」入口与滚动锚定。
- 手机遥控页与桌面端实时同步：桌面端 / CLI / 扩展发起的运行会立刻同步到手机（提问、运行状态、排队），SSE 重连、网络恢复、运行中静默超时都会补一次快照。
- 手机遥控页可以回答扩展提问（`ask_user` 等阻塞请求）：卡片钉在输入区上方，`select` / `confirm` / `input` / `editor` 直接在手机上作答，`custom` 至少可以取消放行；不回答会一直挂着，以前在手机上完全看不见。
- 新增 SDK 内置模型目录支持；模型配置支持声明输入能力。

### Changed

- 手机端状态色改用 `--live` / `--danger`，圆角收成 `MOBILE_RADIUS` 一套规则，动效统一在 `prefers-reduced-motion` 下降级。
- 手机遥控页「停止」按钮与状态色、输入框、问答卡片统一走 `MOBILE_RADIUS` 与状态 token；问答卡片的输入控件保持 16px，避免 iOS 聚焦缩放。
- 聊天最小化地图改为独立列，并优化节点布局算法。
- 设置面板整合，移除独立的配置组件。
- 任务进度展示优化，并加载用户 Shell 环境。
- 应用、npm、Tauri 和 Cargo 版本号统一为 `0.85.1-alpha.12`。

### Fixed

- 修复手机遥控页没有流式输出：`message_start` 未转成流式快照，导致流式 reducer 丢弃后续全部增量。
- 修复手机遥控页发出的消息要等整轮结束才显示：发送后先在本地插入用户气泡，快照出现新条目后再交接。
- 修复手机遥控页输入框自增长高度发送后不复位（并把上限收到 3 行）。
- 修复手机遥控页「新建任务」被服务端挑中的会话拉回，以及流式工具入参显示成 `{}`。
- 修复手机端回答扩展提问后，桌面端的弹窗不会关闭：现在 `requestExtensionUi()` 在所有结束路径（作答 / 超时 / 中止 / 取消）的统一出口广播 `extension_ui_resolved`，两端按请求 id 各自收起已作废的弹窗。
- 修复扩展弹窗的输入内容每 15 秒被状态对账重置的问题（改按请求 id 重置，而不是按每次新建的请求对象）。
- 修复 macOS 箭头功能键输入问题。
- 修复 `next build` 在 `Running TypeScript` 阶段报 TS2344 导致 CI 打包失败：`app/api/mobile/pair` 与 `state` 两个 route 额外导出了 `resolvePairPort` / `pickPendingUiRequest`，而 Next 16 的 route 类型只允许导出 HTTP 方法与约定字段；两个函数已移到 `lib/mobile-pair.ts` 与 `lib/mobile-state.ts`。
- 修复侧边栏更新入口只剩图标、看不出有哪个版本的问题：恢复为显示 `↑ v<最新版本>` 的胶囊按钮。
- 修复移动端悬浮工具栏丢失主题与语言入口的问题：主题按钮按当前偏好切换，语言按钮恢复为下拉选择。
- 修复「手机查看」入口在新建会话（还没有 sessionId）时就出现的问题：那时二维码只能指向该项目里最近的活动会话，扫出来是别的会话；现在等会话产生后再显示。

## [0.85.1-alpha.8] - 2026-09-10

### Added

- 增加在系统文件管理器中定位当前预览文件的功能。
- macOS DMG 增加 `Fix Pi Desktop.command` 启动修复辅助工具。

### Changed

- 聊天输入栏改为队列菜单，并改进面板切换时的滚动行为。
- 应用、npm、Tauri 和 Cargo 版本号统一为 `0.85.1-alpha.8`。
- `package.json` 作为唯一版本源，`npm run version:set -- <version>` 会同步全部派生版本字段。

### Fixed

- 修复发布 tag 与项目版本号不一致导致版本检查失败的问题。
- MCP 连通性测试改为发送实际应用版本，不再使用硬编码版本号。

## [0.85.1-alpha.7] - 2026-09-09

### Added

- 增加统一版本同步脚本 `npm run version:sync`。
- 增加版本一致性检查 `npm run version:check`，并接入 GitHub Actions。
- 更新入口改为图标显示，完整版本号通过悬停提示查看。

### Changed

- Pi 核心包升级至 0.85.1：`pi-agent-core`、`pi-ai`、`pi-coding-agent`、`pi-tui`。
- APIsets 的 `gpt-*` 模型改用 OpenAI Responses API，其他模型继续使用 Anthropic Messages API。
- 应用、npm、Tauri 和 Cargo 版本号统一为 `0.85.1-alpha.7`。

### Fixed

- 修复 Responses API 已发送终态事件但 SSE 连接未关闭时，Agent 状态持续运行的问题。
- 修复 Pi 0.85.1 更新后纯文本主题缺少基础颜色导致 RPC 初始化失败的问题。
- 修复 Windows CRLF 换行导致版本检查误报不一致的问题。
